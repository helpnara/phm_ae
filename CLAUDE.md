# CLAUDE.md

이 파일은 이 저장소에서 Claude Code(claude.ai/code)가 작업할 때 참고하는 프로젝트 가이드다.

## 프로젝트 개요

PHM(예지정비) 분야의 **오토인코더 기반 이상탐지 시스템**. 핵심 아이디어:
정상 데이터가 풍부하고 이상 데이터가 희소한 불균형 상황에서, **정상 데이터만 오토인코더로 학습**시켜 정상 패턴의 복원 함수를 만든다. 이상 데이터가 들어오면 입력과 복원 출력의 차이(**재구성 오차**)가 커지므로, 이 오차를 임계값과 비교해 이상을 탐지한다.

최종 목표: **CSV 파일을 업로드하면 웹에서 이상 여부를 테스트**할 수 있는 형태.

상세 요구사항은 `docs/requirements.md` 참조.

## 핵심 설계 원칙 (반드시 지킬 것)

1. **데이터 누수 방지**: 스케일러(정규화 파라미터)는 **정상 학습 데이터에서만** `fit`하고 저장한다. 추론/검증/테스트에서는 저장된 스케일러로 `transform`만 한다.
2. **정상 데이터 순수성**: 학습용 "정상" 데이터에 이상이 섞이지 않았는지 학습 전 점검한다.
3. **임계값은 아티팩트**: 임계값은 정상 데이터 재구성 오차 분포에서 산출(기본 99 백분위수)해 저장한다. 하드코딩 금지.
4. **AE 과용량 경계**: 병목이 너무 크면 이상치까지 복원해 탐지가 실패한다. 병목 차원·정규화를 신중히.
5. **불균형 평가**: 정확도(accuracy)로 평가하지 말 것. F1, PR-AUC, ROC-AUC, 혼동행렬 사용.
6. **아티팩트 세트**: 모델 + 스케일러 + 임계값 + 스키마 + 메타데이터를 한 세트/버전으로 저장해 재현성 보장.
7. **스키마 검증**: 학습 시 컬럼 스키마를 저장하고, 추론 시 업로드 CSV를 이와 대조해 검증한다.
8. **설명가능성**: 샘플별 이상 점수뿐 아니라 **피처별 재구성 오차**를 제공해 원인(어느 센서) 진단을 돕는다.

## 프로젝트 구조 (계획)

```
phm_ae/
├── CLAUDE.md                # 이 파일
├── README.md                # 사용자용 설명(예정)
├── requirements.txt         # 의존성(예정)
├── docs/
│   └── requirements.md      # 요구사항 정의서
├── config/                  # 하이퍼파라미터/경로 설정(예정)
├── src/
│   ├── data.py              # 로딩·스키마 검증·전처리(예정)
│   ├── model.py             # 오토인코더 정의(예정)
│   ├── train.py             # 학습 + 아티팩트 저장(예정)
│   ├── threshold.py         # 임계값 산출(예정)
│   ├── inference.py         # 추론 파이프라인(예정)
│   └── evaluate.py          # 평가지표(예정)
├── app/                     # 웹 앱(예정)
├── artifacts/               # 저장된 모델/스케일러/임계값/스키마(gitignore 권장)
└── data/                    # 데이터(gitignore 권장)
```

> 현재 상태: 요구사항 정의서와 본 가이드 문서만 존재. 코드는 마일스톤(M1~M6)에 따라 추가.

## 기술 스택 (기본값)

- Python 3.10+
- TensorFlow/Keras (또는 PyTorch — 하나로 고정)
- pandas, numpy, scikit-learn
- 웹: Streamlit(빠른 테스트) 또는 FastAPI+프론트(확장)
- 시각화: matplotlib/plotly

프레임워크·웹 스택은 `docs/requirements.md` §14 "열린 결정사항"에서 확정한다. 임의로 바꾸지 말고 확정된 선택을 따른다.

## 개발 워크플로 규칙

- 작업 브랜치: `claude/phm-autoencoder-anomaly-3uz6eh` (지정 브랜치에서만 개발)
- 커밋 메시지는 명확하고 서술적으로.
- 새 코드는 주변 코드의 스타일/네이밍/주석 밀도를 따른다.
- 데이터·아티팩트 대용량 파일은 커밋하지 않는다(.gitignore).
- 재현성: 랜덤 시드 고정, 의존성 버전 고정.

## 실행 방법 (구현 후 갱신)

```bash
# 예정: 학습
# python -m src.train --config config/default.yaml

# 예정: 웹 앱
# streamlit run app/app.py     (Streamlit 채택 시)
# 또는 uvicorn app.main:app    (FastAPI 채택 시)
```

## 참고

- 요구사항 상세: `docs/requirements.md`
- 이상탐지 판정 로직: 재구성 오차 ≥ 임계값 → 이상, 미만 → 정상
