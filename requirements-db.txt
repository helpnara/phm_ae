# 사내 Oracle 연동용 추가 의존성 (선택).
# 로컬/합성 데이터 테스트나 클라우드 데모에는 불필요.
# 사내 서버 이식 시에만 설치:  pip install -r requirements.txt -r requirements-db.txt
# python-oracledb는 기본 thin 모드로 Instant Client 없이 동작(docs/migration_notes.md §1,3).
oracledb>=2.0
SQLAlchemy>=2.0
