# 진행 현황 & 향후 계획 (ROADMAP / TODO)

이 문서는 프로젝트의 **진행 현황**과 **다음 할 일(TODO)**을 정리한다.
완료 상세 이력은 [`CHANGELOG.md`](CHANGELOG.md), 요구사항은 [`docs/requirements.md`](docs/requirements.md) 참고.

- 최종 갱신: 2026-07-22
- 브랜치: `claude/phm-autoencoder-anomaly-3uz6eh` · 리뷰 PR: **#1** (base `main`)
- 실행: `pip install -r requirements.txt && streamlit run app/app.py`

---

## ✅ 지금까지 완료 (진행 현황)

### 기반 · 파이프라인
- [x] 요구사항 정의서 / 개발 가이드(CLAUDE.md) / 스택 확정(행 단위 Dense AE·Streamlit·TF/Keras)
- [x] 전처리(스키마·스케일러 저장, 데이터 누수 방지) · 임계값 아티팩트화
- [x] 오토인코더: **Dense(행 단위)** + **LSTM-AE(시계열)** — EDA 시간의존성 진단 후 팝업 선택
- [x] 추론·평가(F1/PR-AUC/ROC-AUC/혼동행렬) · 피처별 오차 원인 진단(설명가능성)
- [x] 합성 데이터 생성(파일 + 메모리 빌더: 행단위/시계열)

### 웹앱 — 좌측 메뉴 4단 구성
- [x] **1. EDA** — CSV 업로드/샘플 생성 → 통계·분포·상관·시간의존성(단계별 해석 가이드)
- [x] **2. 모델 생성** — 학습 설정·진행률·LSTM 팝업·학습 품질(손실/오차분포/ROC·PR)
- [x] **3. 모델 평가** — 저장모델 로드 → 단일 샘플 판정 / 테스트셋 파일 평가 / 모델 비교
- [x] **4. 성능 모니터링** — 운영데이터 드리프트 감지(구간별 오차·이상률, 경고) *(기본 화면)*
- [x] 사이드바는 **메뉴 내비게이터만** (중복 기능은 각 페이지로 이동)

### 모델 관리 · 편의
- [x] **모델 레지스트리**: 학습 모델 영구 저장·목록·선택·삭제·이름/메모/태그 편집
- [x] **모델 ↔ 데이터 호환성 배지**, **모델 비교**(동일 데이터로 지표·ROC/PR) + **최고 모델 활성 지정**
- [x] **F1 최대 임계값 자동 추천**, 임계값 슬라이더(모델 전환 시 자동 보정)
- [x] 결과 CSV · **HTML 리포트** · **학습 아티팩트 zip** 다운로드, **다중 CSV 배치 판정**

### 배포 · 사내 이관 준비
- [x] Streamlit Cloud 원클릭 배포 배지 · 클라우드 의존성 정리(tensorflow-cpu, DB 의존성 분리)
- [x] Oracle 연동 스켈레톤(`src/db.py`·`batch_infer.py`·`train --sql`) + 이식 노트
- [x] 문서: CHANGELOG · ROADMAP · README(실행 화면) · migration_notes

---

## 📋 다음 할 일 (TODO)

### P3 후속 — 운영/모니터링 고도화 (다음 우선)
- [ ] **드리프트 모니터링 세부 조정** *(리뷰 피드백 반영)*
  - [ ] 판정 방식 옵션(z-score 외 KS/PSI 분포검정·이동평균)
  - [ ] 구간 기준(시/일/주 리샘플·기간 필터), 기준선(baseline) 재설정 옵션
- [ ] **재학습 자동 트리거 · 알림**(이메일/사내 메신저) — 임계 초과 시
- [ ] **레지스트리 내보내기/가져오기(zip)** — 여러 모델 일괄 사내 이관

### P4 — 사내 서버/DB 연동 (실배포)
- [ ] Oracle 실연동(접속·조회·**웹 판정결과 결과테이블 적재**) — 스켈레톤 실환경 검증
- [ ] 권한/접근 로그(프록시 인증 + 감사 로그)

### 소규모 후속 (틈틈이)
- [ ] 모델 비교 결과 내보내기 / 비호환 모델도 표시 / 레지스트리 성능순 정렬
- [ ] 태그로 모델 필터링 / 단건 수동 판정 다중행 입력
- [ ] 메뉴에 진행 상태 표시(데이터 준비됨·모델 N개) / 첫 방문 온보딩 안내

### 백로그(중장기)
- [ ] 임계값 정책 다양화(비용 가중 F-beta 등)·임계값 A/B 비교
- [ ] 피처 선택/차원 축소 사전 단계, 다중 피처셋 프로파일
- [ ] 리포트 PDF 직접 출력(kaleido) — 현재는 HTML→인쇄로 대체
- [ ] 대용량 스트리밍/증분 추론·캐싱 최적화

---

## 진행 방식
- 각 항목: 구현 → 실제 앱 구동/테스트로 검증 → 커밋(이력은 CHANGELOG 누적) → 화면 공유.
- 우선순위·범위는 매 세션 시작 시 조정. 다음 세션은 **P3 후속(모니터링 세부 조정 → 알림/재학습)** 부터 권장.
