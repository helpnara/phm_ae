# 향후 업데이트 계획 (ROADMAP)

이 문서는 앞으로 진행할 개선 작업을 정리한다. 완료 이력은 [`CHANGELOG.md`](CHANGELOG.md) 참고.

- 최종 갱신: 2026-07-17
- 현재 상태: 웹앱(EDA·학습·판정·시각화·리포트·배치·모델 레지스트리) 동작. 시계열(LSTM-AE)
  옵션 및 사내 Oracle 연동 스켈레톤 포함. 상세는 CHANGELOG의 2026-07-14 / 2026-07-17 항목.

---

## 다음 세션 시작점 (2026-07-18 예정)
아래 후보를 우선순위 순으로 함께 진행한다. 각 항목은 독립적으로 반영 가능하다.

### P1 — 모델 레지스트리 활용 ✅ 완료 (2026-07-22)
1. ~~**모델 비교 뷰**~~ ✅ — 호환 모델을 현재 데이터로 실행해 지표·ROC/PR 비교.
2. ~~**모델 ↔ 데이터 호환성 배지**~~ ✅ — 사이드바에 ✅ 호환 / ⚠️ 불일치 표시.
3. ~~**비교 → 최고 모델 활성 지정 버튼**~~ ✅ (P1 후속).

### P2 — 사용 편의 ✅ 완료 (2026-07-22)
1. ~~**단건 수동 입력 판정**~~ ✅ — 센서 값 직접 입력→즉시 판정.
2. ~~**모델 이름 변경·메모/태그**~~ ✅ — 사이드바 편집 UI, `entry.json` 확장.

### P3 — 사내 이관/운영
5. **레지스트리 내보내기/가져오기(zip)**
   - 여러 모델을 한 번에 사내 서버로 이관. (단일 모델 zip은 이미 지원)
6. **드리프트 모니터링** ✅ 기본 화면 완료 (2026-07-22)
   - 운영 데이터 업로드 → 구간별 오차·이상률 추이, 학습 기준 대비 드리프트 경고.
   - 남은 확장: 재학습 **자동** 트리거·알림(이메일/메신저), 기간 필터, 기준선 재설정 등.
7. **Oracle 연동 판정결과 적재(웹)**
   - 배치(`src/batch_infer.py`)에 더해 웹 판정결과도 결과 테이블 적재 옵션.
8. **권한/접근 로그**
   - 사내 배포 시 프록시 인증 + 감사 로그(`docs/migration_notes.md §7~8`).

---

## 백로그(중장기 아이디어)
- 임계값 정책 다양화(비용 가중 F-beta 등) 및 임계값 A/B 비교.
- 피처 선택/차원 축소 사전 단계, 다중 피처셋 프로파일 지원.
- 리포트 PDF 직접 출력(kaleido 등) — 현재는 HTML→브라우저 인쇄로 대체.
- 대용량 스트리밍/증분 추론, 캐싱 최적화.
- 레이아웃: EDA/학습 결과 탭 전환 옵션(스크롤 최소화) — 사용자 선호 확인 후.

---

## 진행 방식
- 각 항목은 반영 → 실제 앱 구동/테스트로 검증 → 커밋(이력은 CHANGELOG에 누적)한다.
- 브랜치: `claude/phm-autoencoder-anomaly-3uz6eh`.
- 우선순위/범위는 매 세션 시작 시 조정 가능.
