# 진행 현황 & 향후 계획 (ROADMAP / TODO)

이 문서는 프로젝트의 **진행 현황**과 **다음 할 일(TODO)**을 정리한다.
완료 상세 이력은 [`CHANGELOG.md`](CHANGELOG.md), 요구사항은 [`docs/requirements.md`](docs/requirements.md) 참고.

- 최종 갱신: 2026-07-22
- 브랜치: `claude/phm-autoencoder-anomaly-3uz6eh` · 리뷰 PR: **#1** (base `main`)
- 실행: `pip install -r requirements.txt && streamlit run app/app.py`

---

## ✅ 지금까지 완료 (진행 현황)

### 기반 · 파이프라인
- [x] 요구사항 정의서 / 개발 가이드(CLAUDE.md) / 스택 확정(행 단위 Dense AE·Streamlit·TF/Keras)
- [x] 전처리(스키마·스케일러 저장, 데이터 누수 방지) · 임계값 아티팩트화
- [x] 오토인코더: **Dense(행 단위)** + **LSTM-AE(시계열)** — EDA 시간의존성 진단 후 팝업 선택
- [x] 추론·평가(F1/PR-AUC/ROC-AUC/혼동행렬) · 피처별 오차 원인 진단(설명가능성)
- [x] 합성 데이터 생성(파일 + 메모리 빌더: 행단위/시계열)

### 웹앱 — 좌측 메뉴 4단 구성
- [x] **1. EDA** — CSV 업로드/샘플 생성 → 통계·분포·상관·시간의존성(단계별 해석 가이드)
- [x] **2. 모델 생성** — 학습 설정·진행률·LSTM 팝업·학습 품질(손실/오차분포/ROC·PR)
- [x] **3. 모델 평가** — 저장모델 로드 → 단일 샘플 판정 / 테스트셋 파일 평가 / 모델 비교
- [x] **4. 성능 모니터링** — 운영데이터 드리프트 감지(구간별 오차·이상률, 경고) *(기본 화면)*
- [x] 사이드바는 **메뉴 내비게이터만** (중복 기능은 각 페이지로 이동)

### 모델 관리 · 편의
- [x] **모델 레지스트리**: 학습 모델 영구 저장·목록·선택·삭제·이름/메모/태그 편집
- [x] **모델 ↔ 데이터 호환성 배지**, **모델 비교**(동일 데이터로 지표·ROC/PR) + **최고 모델 활성 지정**
- [x] **F1 최대 임계값 자동 추천**, 임계값 슬라이더(모델 전환 시 자동 보정)
- [x] 결과 CSV · **HTML 리포트** · **학습 아티팩트 zip** 다운로드, **다중 CSV 배치 판정**

### 배포 · 사내 이관 준비
- [x] Streamlit Cloud 원클릭 배포 배지 · 클라우드 의존성 정리(tensorflow-cpu, DB 의존성 분리)
- [x] Oracle 연동 스켈레톤(`src/db.py`·`batch_infer.py`·`train --sql`) + 이식 노트
- [x] 문서: CHANGELOG · ROADMAP · README(실행 화면) · migration_notes

---

## 🎯 유의미한 결과 도출을 위한 과제 (2026-07-24 검토)

현재는 "기능이 도는" 단계다. 사내 실데이터에서 **신뢰할 수 있는 수치**를 얻으려면 아래가 필요하다.

### V1. 임계값 산출 방식 교정 ⭐ (가장 시급 · 사내 데이터 없이 가능)
- **문제**: 지금은 임계값을 **모델이 학습한 그 데이터**의 오차 분포에서 산출한다(`train.py`).
  모델은 학습 데이터를 이미 잘 복원하므로 오차가 **낙관적으로 작게** 나오고, 임계값도 과소 설정된다.
  → 실제 운영의 *처음 보는 정상 데이터*는 오차가 더 커서 **오경보(false alarm)가 예상보다 많아진다.**
- **해결**: 정상 데이터를 학습용/보정용으로 분리(예: 80/20) → 학습은 학습용만, **임계값은 보정용
  (학습에 안 쓴 정상)** 에서 산출. 프로젝트 원칙 #1(데이터 누수 방지)과도 일치.
- **효과**: 오경보율이 현실적인 수준으로 보정됨. 지금 바로 반영 가능.

### V2. 평가 프로토콜 정립 (사내 데이터 없이 설계 가능)
- 현재는 같은 데이터로 임계값 산출·평가 → 성능이 낙관적.
- **시간 기반 분할**(과거 학습 → 미래 평가)로 바꿔야 실제 운영 성능에 가깝다.
- 이상 유형별(마모/스파이크/수준이동) 검출률, **탐지 지연(detection latency)** 지표 추가.

### V3. 운영 관점 지표·정책 (사내 데이터 없이 가능)
- **알람 디바운싱**: 1건 이상만으로 알람하면 오경보 폭증 → "연속 N건/M분 내 K건" 규칙 필요.
- **오경보율(FAR) 기준**: "월 몇 건 이하" 같은 운영 목표에서 역산해 임계값 결정.
- 비용 가중 지표(미탐 vs 오탐 비용 비율)로 임계값 정책 선택.

### V4. 실데이터 특성 반영 (사내 데이터 확보 후)
- **다중 운전 모드**: 정상 상태가 여러 개(가동/정지/부하별)면 단일 AE로는 부정확
  → 모드 구분 후 모드별 모델 또는 모드를 피처로 투입.
- **정상 구간 정의**: 정비 이력·가동 로그로 "확실한 정상" 구간을 라벨링해야 학습 데이터 순수성 확보.
- **학습 데이터 양·기간**: 계절/부하 변동을 포함하도록 충분한 기간 확보(권장: 최소 수 주~수 개월).
- **센서 선택**: 고장과 무관한 센서는 노이즈 → EDA 상관/기여도로 선별.

### V5. 검증용 현실적 데이터셋 (사내 데이터 없이 가능)
- 현재 합성 데이터는 이상이 뚜렷해 모든 모델이 F1 0.95+ → **모델 간 변별이 안 된다.**
- 난이도 조절(미세 이상·점진적 열화·다중 운전모드) 가능한 생성기를 만들면
  기능·튜닝 검증이 실제와 가까워진다.

---

## 📋 다음 할 일 (TODO)

### P3 후속 — 운영/모니터링 고도화 (다음 우선)
- [x] **드리프트 모니터링 세부 조정** ✅ (2026-07-22~23)
  - [x] 판정 방식 3종(z-score / PSI / KS) 선택
  - [x] 구간 기준(시/일/주 리샘플), 기준선(baseline) 재설정
- [x] **레지스트리 내보내기/가져오기(zip)** ✅ — 전체 모델 일괄 사내 이관
- [ ] **재학습 자동 트리거 · 알림**(이메일/웹훅) — *현장 적용 단계에서 연동*(사내 정보 필요)
  - 현재: 드리프트 경고 시 **재학습 권고**(수동)까지 구현

### P4 — 사내 서버/DB 연동 (실배포)
- [ ] Oracle 실연동(접속·조회·**웹 판정결과 결과테이블 적재**) — 스켈레톤 실환경 검증
- [ ] 권한/접근 로그(프록시 인증 + 감사 로그)

### 소규모 후속 (틈틈이)
- [x] 모델 비교 결과 내보내기 / 비호환 모델도 표시 / 레지스트리 F1순 정렬 ✅
- [x] 태그로 모델 필터링 ✅
- [ ] 단건 수동 판정 다중행 입력
- [ ] 메뉴에 진행 상태 표시(데이터 준비됨·모델 N개) / 첫 방문 온보딩 안내
- [ ] 모니터링 기간(날짜) 필터 · 구간 표 드리프트 열

### 백로그(중장기)
- [ ] 임계값 정책 다양화(비용 가중 F-beta 등)·임계값 A/B 비교
- [ ] 피처 선택/차원 축소 사전 단계, 다중 피처셋 프로파일
- [ ] 리포트 PDF 직접 출력(kaleido) — 현재는 HTML→인쇄로 대체
- [ ] 대용량 스트리밍/증분 추론·캐싱 최적화

---

## 진행 방식
- 각 항목: 구현 → 실제 앱 구동/테스트로 검증 → 커밋(이력은 CHANGELOG 누적) → 화면 공유.
- 우선순위·범위는 매 세션 시작 시 조정. 다음 세션은 **P3 후속(모니터링 세부 조정 → 알림/재학습)** 부터 권장.
