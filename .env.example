# 사내 Oracle DB 접속정보 예시. 실제 값으로 채워 export 하거나 .env로 로드한다.
# 이 파일(.env.example)은 예시이며, 실제 값이 담긴 .env는 커밋하지 말 것.
#   (.gitignore에 .env 등록됨)

export PHM_DB_USER=phm
export PHM_DB_PASSWORD=change_me
# DSN: "host:port/service_name" 형태 또는 tnsnames.ora 별칭
export PHM_DB_DSN=oracle-host:1521/ORCLPDB1
