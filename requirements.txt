# 웹 데모/클라우드 배포용 의존성 (Streamlit Community Cloud 호환)
# - tensorflow-cpu: CUDA(nvidia-*) 미포함으로 설치 가볍고 빠름(추론/소형 AE에 충분)
# - 상한 핀 없음: 배포 호스트의 Python 버전(3.11~3.13)에 맞는 휠을 pip가 선택
# 사내 Oracle 연동 의존성은 requirements-db.txt 참고(데모에는 불필요).
numpy>=1.26
pandas>=2.1
scikit-learn>=1.4
scipy>=1.11
tensorflow-cpu>=2.16
streamlit>=1.36
plotly>=5.18
joblib>=1.3
PyYAML>=6.0
